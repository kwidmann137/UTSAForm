$(function(){
	$('.modal').modal({
		show: false,
		backdrop: 'static'
	});

	// $('textarea', document).addClass('animated');
	// $('.normal').autosize();
	// $('.animated').autosize({append: "\n"});
});